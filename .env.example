# Set to 'true' for demo mode (auto-seeds sample data, shows demo credentials)
DEMO_MODE=true

# Required in production - generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=

# PostgreSQL connection string (provided by Railway, or local)
DATABASE_URL=postgresql://user:password@localhost:5432/cath_lab

# Node environment
NODE_ENV=development
